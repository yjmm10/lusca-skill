# CHANGELOG — lusca-paper-blog

所有版本变更记录。版本号规则：MAJOR（流程/输出规范 breaking）/ MINOR（新节、新范式、新 reference）/ PATCH（措辞、typo）。

## [1.7.1] - 2026-08-17

### Fixed
- **回溯修正 `outputs/lusca-paper-blog/` 下 9 篇旧产出的封面/首图不一致**（v1.2.x–v1.3.0 时代生成，早于 v1.4.0 的外链规范）：8 篇正文首图用的是本地路径 `imgs/cover.jpg` 而非 frontmatter `cover` 的外链 URL（封面与首图不一致、且非链接形式），统一改为各自 frontmatter `cover` 的同一外链 URL（本地备份不动，`cover_backup` 已在前置条目补齐）；extractbench 篇正文缺氛围图且无 `cover_backup`，在 H1 后补插 `![氛围图](https://picsum.photos/seed/extractbench/1200/600)`、frontmatter 补 `cover_backup`、下载备份到 `imgs/cover.jpg`。修复后全量自检：所有博客 `cover` == 正文首图 == http(s) 外链、`cover_backup` 齐全、check_imgs.py 无孤儿图。顺手：2407/flashattention-3 补嵌历史漏嵌的 `fig6b_fp16_bwd_hd128.jpg`（FP16 后向图，补在 fig5e 图组后）；SKILL.md 与模板文末出处块示例的过期版本号 v1.6.0 同步为 v1.7.1。

## [1.7.0] - 2026-08-14

### Fixed
- **封面图 1–2 天内重复修复：`fetch_cover.py` 改为登记去重版**。v1.6.0 用 slug+timestamp 做 MD5 种子从 Bing 候选池选图，但实测该池只有 ≈ 15 张 unique（HPImageArchive idx=0..7 去重后仍是 15，池子扩不大），15 张里哈希撞图概率天然高。改为：合并 Bing 池（≈ 15 张）+ picsum.photos 全量池（`v2/list` 前 3 页 ≈ 300 张）为候选，用持久化登记文件 `~/.cache/lusca-skill/lusca-paper-blog/used_covers.json`（用户级缓存，跨项目跨会话）记录已用图，**每次只从没用过的候选里按种子选**，两池全用完才清空登记循环；离线全取不到退化为带唯一 seed 的 picsum 直链（同 v1.6.0）。登记含 slug 与日期、可人工审计，写失败不影响出图。SKILL.md「图片与视觉」节与 Reference 索引同步。

## [1.6.0] - 2026-08-12

### Fixed
- **封面图重复修复：新增 `scripts/fetch_cover.py` 脚本，禁止手选**。根因是 AI 执行时跳过 `hash(slug) % len(images)` 逻辑、直接取 `images[0]`，导致同一天所有博客撞同一张 Bing 图。脚本用 **slug + 时间戳** 做种子（每次运行时间戳不同 → 每次拿到不同图），从 Bing 候选池（idx=0 + idx=7 合并去重 ≈ 15 张）按 MD5 哈希选图，取不到降级 picsum.photos（seed 同用 slug+timestamp）；自动下载备份到 `imgs/cover.jpg`，输出 JSON（url/backup/source）。SKILL.md「图片与视觉」节改为**必跑脚本、禁止手选**，移除手动的 Bing/picsum URL 构造指令（全由脚本处理）；模板氛围图注释、自查清单、Reference 索引同步。

## [1.5.0] - 2026-08-07

### Added
- **frontmatter 新增 `abstract` 字段（≤200 字中文博文摘要）**：用于平台描述/SEO/列表预览，概括"论文讲了什么、核心贡献、对读者意味什么"；与正文 TL;DR（一句话钩子）互补——abstract 是完整段落、TL;DR 是一句话。SKILL.md「输出与存放」节（字段列表 + `abstract` 说明条）、模板 frontmatter 同步。与 lusca-report-blog v1.5.0 一并加 abstract，两 skill 字段对齐。

## [1.4.0] - 2026-08-07

### Changed
- **frontmatter `cover` 改为存外链 URL（不再是来源描述）**：原本 `cover` 记文字来源描述（`Bing 每日一图 · <copyright>`）、`cover_url` 记外链，两个字段拆分冗余且 `cover` 不是可直接用的链接。合并为 `cover` = 氛围图外链 URL（正文实际引用的图片地址）、`cover_backup` = 本地备份路径、新增可选 `cover_source` 记来源描述（Bing copyright / picsum seed，用于溯源/版权标注）。SKILL.md「图片与视觉」「输出与存放」节、模板 frontmatter / 氛围图注释 / 自查清单同步，并加**硬约束**：`cover` 必须是 http(s):// 开头图片直链，❌ 严禁 `Bing 每日一图 · …` 文字描述（落盘前自检 `cover` 以 http 开头）。顺手修正模板示例出处块停留在 v1.2.8 的过期版本号。
- **新增 `category` 字段（单一主分类）+ `tags` 改用泛化技术标签**：`category` 单选技术大类（人工智能 / 大模型 / 机器学习 / 深度学习 / NLP / 计算机视觉 / 多模态 / 强化学习 / RAG / Agent / 智能文档 / 知识图谱），`tags` 选 2-4 个泛化标签（LLM / AIGC / 人工智能 / 智能文档 等）——不造 ExtractBench、某模型代号这类论文专属细标签（只进标题与正文）。推荐清单写在 SKILL.md「输出与存放」节，模板 frontmatter 与自查清单同步加校验条。
- **措辞稳重化：「一句话带走」→「一句话总结」、「冷静一下」→「理性看待」**：原措辞偏口语随意。TL;DR 引导词改「一句话总结」（保留"一句话"的轻量感，"总结"比"带走"稳重）；caveat 节标题改「理性看待」（承接"让读者别上头"的本意，比"冷静一下"正式，仍贴合博客风格）。SKILL.md「结构」节、模板顶部注释 / TL;DR 占位 / caveat 节标题 / 结尾节标题同步。

### Fixed
- **回溯修正 `outputs/lusca-paper-blog/` 下 9 篇旧产出的 frontmatter**（旧规则生成，cover 曾是文字描述、缺 `category`、tags 为论文专属细标签）：cover 改为真实图片直链——8 篇「穴鸮/非斯」对应 Bing API 实际 URL，extractbench 原无 cover 补 `picsum.photos/seed/extractbench`；补 `category`（大模型 / 智能文档 / Agent / 多模态）、`cover_source`（原文字描述）、`cover_backup`（imgs/ 已有备份的 8 篇）；tags 从 `document-parsing`/`vlm`/`ocr` 等细标签改为 `LLM`/`深度学习`/`智能文档`/`多模态`/`RAG` 等泛化标签；正文「一句话带走」→「一句话总结」、「冷静一下」→「理性看待」同步（含 2 处正文引用节名处）。

## [1.3.0] - 2026-08-06

### Changed
- **外部氛围图：同一天不固定同一张 + 外链保留并本地备份**：Bing 每日一图从 `n=1`（只取今日一张，篇篇撞图）改为 `n=8` 取最近 8 天候选池、用 `hash(slug) % len` 按 slug 挑一张；picsum 备选的 seed 改用 slug（不再用固定字面量）——不同博客 slug 不同即拿到不同图、同一博客 re-run 稳定。氛围图在正文**直接用外链 URL**（远程即看即载，不换本地路径），**同时下载一份到 `imgs/cover.<ext>` 做备份**（外链失效兜底）；frontmatter 新增 `cover_url`（外链）与 `cover_backup`（本地备份），`cover` 仍记来源描述。SKILL.md「图片与视觉」节、模板 frontmatter / 氛围图示例 / 顶部注释 / 自查清单同步。

## [1.2.8] - 2026-08-04

### Fixed
- **澄清"一张图"只约束外部氛围图**：上一版（v1.2.4）"只需要一张图"被误执行为"全文只留一张、删掉论文原图"。本版明确：外部氛围图（Bing/picsum）开头一张无 caption；论文原图（结果图/架构图/pipeline）该嵌就嵌、配白话 caption，不限张数。SKILL.md「图片与视觉」节重写区分两类图，当前博客恢复 3 张论文原图（三大基准 / DOM 合成 / 跨任务）嵌入。

## [1.2.7] - 2026-08-04

### Changed
- **「通俗」准则细化：保留专业名词让人学到**：明确通俗≠把术语全换成大白话——专业名词保留、首次出现配「术语（白话解释）」；评测指标报数字时附"是什么量、越高/越低越好"判读方向。模板顶部注释与自查清单同步。当前博客给 TEDS / CDM / AP / InChI / SMILES / GRPO 等术语补了白话判读。

## [1.2.6] - 2026-08-04

### Changed
- **「一句话带走」从结尾移到开头作 TL;DR**：放氛围图后、导语前，用引用块给核心结论，让读者一眼判断要不要读；结尾不再重复（避免首尾冗余）。模板结构同步（开头加 TL;DR 占位、结尾一句话带走节改为"已移开头"说明），SKILL.md「结构」节加一条，自查清单加一条，当前博客产出同步。

## [1.2.5] - 2026-08-04

### Changed
- **emoji 放宽到正文**：从"仅资源块 + 关键处少量"改为"正文适当点缀"——H2 小标题配一个语义 emoji、列表 bullet 关键词配相关 emoji、资源块平台 emoji 保留；约束不变：数字/基准结果/caveat 严肃处不加、语义相关不重复、不堆砌。模板顶部注释与自查清单同步。当前博客产出按新准则加了 emoji。

## [1.2.4] - 2026-08-04

### Changed
- **图片策略改为轻视觉**：开头放一张氛围图（H1 后、无下方 caption），论文原图默认不嵌入（按需才加）。氛围图两种来源、**默认 Bing 每日一图**（HPImageArchive API 取今日图），picsum.photos/Unsplash 作备选降级。图来源记 frontmatter 新增 `cover` 字段溯源，图下方不放说明。移除上一版"Bing 慎用"措辞（用户确认默认用 Bing）。模板 frontmatter 加 cover、正文开头加图占位、自查清单更新。

## [1.2.3] - 2026-08-04

### Added
- **新增「视觉：艺术配图点缀 + 必要的 emoji」核心准则**：可用无版权艺术氛围图（picsum.photos/Unsplash，免费商用）点缀导语破冰，增加视觉多样性；Bing 每日一图艺术感强但 Getty 版权、标注"仅限壁纸"，商用慎用；取不到则降级只用论文原图 + emoji。emoji 必要不堆砌（资源块平台 emoji、关键处点缀，H1/数字/caveat 不加）。模板资源块示例与顶部注释同步。

## [1.2.2] - 2026-08-04

### Added
- **新增「结构：分点用列表，段间要连贯」核心准则**：并列分点（"第一/第二""一是/二是""首先/其次/最后"或顿号罗列）一律拆成 markdown 无序列表，3 个及以上必须列表化；列表前后要有引出句和收束句，段间用承接词串成一条线，避免零散堆砌。模板自查清单同步加两条。

### Changed
- infinity-parser2 博客按新准则重写：流水线步骤、问题两个瓶颈、训练两步、可验证奖励三类、三大基准、元素级结果、为什么关心的三点建议、冷静一下的三个 caveat 全部列表化，并补强段间承接。

## [1.2.1] - 2026-08-04

### Changed
- **项目资源块格式**：从"一行用 ｜ 分隔、裸 URL"改为"引用块内每条一行、markdown 链接 `[显示文字](URL)`、显示文字用平台/资源名（如 `GitHub · org/repo`）"。更可读、更贴博客与公众号排版。模板示例与博客产出同步更新。

## [1.2.0] - 2026-08-04

### Added
- **新增「项目资源块」准则**：博客导语后、正文前给一行项目链接（论文/代码/数据集/项目主页），用博客语气提示语领起，方便开发者动手；链接从论文搜集，无则省略不编造。
- **新增「观点：要有自己的思考，别只当复读机」核心准则**：鼓励作者给解读/判断/温和质疑（"我倾向于认为""这里我有个怀疑"），让博客有立场和人味；边界是基于事实的解读，不是编造数据，推测要标明。
- **新增「语气：可以自问自答，可以轻度打趣」核心准则**：鼓励用自问自答推进、轻度打趣拉近距离、自我互动的引子破冰，让博客读起来像有人在跟你聊；明确边界——不打趣到油滑、不在 caveat/数字/原理处抖机灵、密度别高。
- 输入新增**论文地址（arXiv ID / URL / PDF / 粘贴文本）作为一等来源**：与 parse 结果、read 笔记三种并列，skill 用 WebFetch/Read 直接读全文写博客，不再是"兜底"措辞。

### Changed
- **小标题去掉指导性括注**：模板与准则明确 H2 用干净直白问句，不带「（不钻细节）」「（小段 caveat）」这类写法提示——那是模板注释，不是成文标题。模板相应改掉两处带括号的小标题。
- description 补充"自问自答与轻度打趣的互动语气"与"支持直接给论文地址"。
- 模板顶部原则注释、导语/思路节写法指导、自查清单同步更新（加"小标题无指导括注""有互动语气"两条）。

## [1.1.0] - 2026-08-03

### Changed
- **输入来源调整**：默认基于 `lusca-pdf-parse` 解析结果（忠实原文 md + 图片，`outputs/lusca-pdf-parse/{slug}/parsed/`），`lusca-paper-read` 精读笔记作为第二种来源，原始论文兜底。原"优先复用 read 笔记"改为"默认 parse 结果"——parse 是主链中段、最贴近论文原文的事实层，博客事实层以原文为准。
- **受众与共鸣校准**：读者明确为开发者/技术从业者；共鸣主线从"生活场景/普适经验"改为"领域痛点 + 技术圈共识"，生活类比仅用于解释具体技术概念，不作共鸣主线。
- **标题准则**：用论文关键词/简称（不塞完整冗长标题）+ 面向开发者的钩子；标题范式第 2 种改"痛点提问式"、第 4 种从"生活类比式"改"悬念式"。

### Added
- 与体系分工表补 `lusca-pdf-parse` 行；衔接说明改为"在主链中的位置"叙述（parse 还原 / read 读透 / blog 讲好）。
- 模板自查清单加"共鸣主线是行业痛点"一条。

## [1.0.0] - 2026-08-03

### Added
- 首版：论文博客化解读技能。输入 `lusca-paper-read` 精读笔记（优先复用，含 imgs/）或论文本身，产出 8-12 分钟阅读的通俗科普博客。
- **核心准则**：通俗（术语配白话、公式只讲用途不讲推导）、深入浅出（why→how→what + 生活类比）、生活共鸣（导语用场景或近期新闻切入）、篇幅 3500-6500 字、每段 ≤4-5 行、活泼标题（含论文名 + 单个钩子）。
- `assets/blog-template.md`：博客结构骨架（导语 / 问题 / 思路 / 效果 / 为什么关心 / caveat / takeaway）+ 5 种标题范式 + frontmatter + 各节字数指导 + 落盘前自查清单。
- 与 `lusca-paper-read` 衔接：优先复用精读笔记与 imgs/，省去重读；无笔记时直接读论文。
- 文末出处块、段落不硬换行等文档输出规范遵 CLAUDE.md。
